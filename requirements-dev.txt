# Test-only dependencies.
#
# Deliberately NOT `-r requirements.txt` — the test suite is designed to
# run against synthetic data and mocked/injected fakes (see README's
# "Testing philosophy"), so it never needs ultralytics, torch, mlflow, or
# gradio installed. Pulling those in here would make CI slow and defeat
# the point. Only list what tests actually import.
pytest>=8.0
httpx>=0.27
fastapi>=0.110
python-multipart>=0.0.9
pyyaml>=6.0
pillow>=10.0
numpy>=1.24
